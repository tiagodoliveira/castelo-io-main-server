package io.castelo.main_server.end_device_model;

import org.springframework.data.jpa.repository.JpaRepository;

public interface EndDeviceModelRepository extends JpaRepository<EndDeviceModel, Integer> {
}
